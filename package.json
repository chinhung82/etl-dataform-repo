{
  "name": "dataform-example",
  "version": "1.0.0",
  "description": "Dataform ETL pipelines for BigQuery (bronze → silver → gold)",
  "scripts": {
    "build": "dataform compile",
    "run": "dataform run",
    "test": "dataform test"
  },
  "dependencies": {
    "@dataform/core": "3.0.0"
  },
  "devDependencies": {}
}