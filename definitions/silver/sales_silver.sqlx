config {
  type: "table",
  schema: "ds1"
}

SELECT
  date,
  store_id,
  product_id,
  product_category,
  quantity,
  unit_price,
  quantity * unit_price AS total_amount
FROM
  ${ref("ds1", "sales_bronze")}
WHERE quantity > 0
  
